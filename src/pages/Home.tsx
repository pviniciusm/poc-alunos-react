import React from 'react';
import DefaultLayout from '../config/layout/DefaultLayout';

const Home: React.FC = () => {
  return (
    <DefaultLayout>
      <h1>Bem vindo!</h1>
    </DefaultLayout>
  );
};

export default Home;
